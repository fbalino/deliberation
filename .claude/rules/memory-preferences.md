# Memory Preferences

- Keep project-specific notes inside `.claude/rules/`.
- Put planning files in the project `plan/` directory.
- For UI work, launch the app and test the result in a browser before handoff.
- Explain outcomes in simple, non-code-heavy language.
